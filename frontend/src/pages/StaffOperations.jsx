import { useState } from "react";
const queues = { "Bidder registration": ["Verify identity and approve bidder access", "Review pending demo bidder for Lot 101"], "Valuation & consignment": ["Review supporting photographs", "Assign specialist and provisional estimate"], "Delivery & disputes": ["Review pickup request", "Resolve delivery question with bidder"], "Content publishing": ["Feature a collection on home page", "Publish an auction story"] };
const StaffOperations = () => { const [active, setActive] = useState(Object.keys(queues)[0]); const [done, setDone] = useState([]); const complete = (task) => setDone((items) => [...items, task]); return <main className="mx-auto max-w-6xl px-4 py-10 sm:px-6"><p className="text-sm font-semibold uppercase tracking-wide text-amber-700">Staff workspace</p><h1 className="mt-2 text-3xl font-bold text-slate-950">Operations queue</h1><p className="mt-3 text-slate-600">Demo queue for administrator workflows. Connect these cards to backend data before public launch.</p><div className="mt-6 grid gap-6 md:grid-cols-[15rem_1fr]"><aside className="flex gap-2 overflow-auto md:flex-col">{Object.keys(queues).map((queue) => <button key={queue} onClick={() => setActive(queue)} className={`rounded-md px-4 py-3 text-left text-sm font-semibold ${queue === active ? "bg-primary text-white" : "border border-slate-200 bg-white text-slate-700"}`}>{queue}</button>)}</aside><section className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="text-xl font-bold">{active}</h2><div className="mt-5 space-y-3">{queues[active].map((task) => <article key={task} className="flex flex-wrap items-center justify-between gap-3 rounded-lg border border-slate-200 p-4"><span className={done.includes(task) ? "text-slate-400 line-through" : "font-medium text-slate-800"}>{task}</span><button disabled={done.includes(task)} onClick={() => complete(task)} className="rounded-md bg-amber-700 px-3 py-2 text-sm font-semibold text-white disabled:bg-slate-300">{done.includes(task) ? "Completed" : "Complete demo task"}</button></article>)}</div></section></div></main>; };
export default StaffOperations;
